# Project Context: Gabinete Ágil

> **Auto-generated by Antigravity Agent** based on repository analysis.

## 1. Tech Stack & Architecture

### **Core Stack**
- **Frontend:** React (Vite) + TypeScript
- **Styling:** CSS Modules / Vanilla CSS (implied from `index.css`) & Tailwind (if detected later, `postcss.config.js` not seen yet but assumed modern)
- **State Management:** React Context + Hooks
- **Backend/DB:** Supabase (PostgreSQL, Auth, Storage, Edge Functions)
- **Deployment:** Vercel (implied from `vercel.json` absence but common with Vite, check build logs)

### **Key Directories**
- `/src`, `/pages`, `/components`: Frontend application logic.
- `/supabase`: Database migrations and configuration.
- `/hooks`: Custom React hooks (`useAuth`, `useSupabase`, etc.).
- `/types.ts`: TypeScript definitions for database schema and app types.

### **Architecture Patterns**
- **Monorepo-ish:** SQL migrations live alongside frontend code.
- **Client-Side Heavy:** Logic resides mostly in React components and hooks interacting directly with Supabase Client.

## 2. Development Guidelines

### **Database (Supabase)**
- Use `.sql` files in root or `/supabase` for schema changes.
- **Policies (RLS):** CRITICAL. All tables must have RLS enabled.
- **Migrations:** Manual or via Supabase CLI.

### **Coding Standards**
- **Strict TypeScript:** No `any`. Define interfaces in `types.ts`.
- **Component Design:** Functional components, Hooks patterns.
- **Error Handling:** Graceful UI degradation, user notifications.

## 3. Current State (Audit)
- **Missing:** Automated tests (Jest/Vitest setup not obvious).
- **Security:** RLS policies present in SQL files, need verification.
- **Onboarding:** `PROJECT_CONTEXT.md` was missing, now created.
