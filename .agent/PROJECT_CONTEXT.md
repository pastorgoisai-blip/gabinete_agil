# Project Context: Gabinete Ágil

> **Auto-generated by Antigravity Agent** based on repository analysis.
> **Last Updated:** 2026-02-19

## 1. Vision & Domain
**Gabinete Ágil** is a SaaS platform for political cabinet management (CRM for politicians).
- **Core Value:** Digitalize the legislative process, manage voters, demands, and agenda.
- **Key Features:** Voter CRM, Kanban Demands, Google Calendar Sync, AI Assistant (Copilot), Document Generation.
- **Architecture:** Multi-tenant SaaS (Tenant = Cabinet) secured by Row Level Security (RLS).

## 2. Tech Stack

### **Frontend (Client-Heavy)**
- **Framework:** React 18 (Vite 6.2.0)
- **Language:** TypeScript 5.8
- **Styling:** TailwindCSS 3.4 + CSS Modules (Legacy `index.css`)
- **State:** React Context API (`AuthContext`, `ProfileContext`)
- **Routing:** React Router DOM 6.23
- **Forms:** React Hook Form + Zod
- **UI Components:** Radix UI (implied), Lucide Icons, Recharts

### **Backend IA & Data (Hybrid)**
- **Primary Backend:** Supabase (PostgreSQL 15+)
    - **Auth:** Supabase Auth (JWT)
    - **Database:** Postgres with `pgvector` enabled (planned/active)
    - **Realtime:** Supabase Realtime
- **Serverless Logic:** Supabase Edge Functions (Deno)
    - `agent-gateway`: WhatsApp Integration
    - `query-copilot`: AI Logic
    - `google-calendar-*`: Sync Logic
- **Python Service (WIP):** Located in `/app`
    - **Status:** Data Models (`app/models`) & Schemas (`app/schemas`) defined.
    - **New Services:**
        - `TenantVaultService`: Manages encrypted government credentials (stored in `cabinets.gov_credentials`).
        - `CryptoService`: Handles Fernet encryption.
        - `DemandService`: Manages demands with City Hall integration.
    - **Libraries:** SQLAlchemy, Pydantic, Cryptography.
    - **Entry Point:** Not currently visible. Services are designed to be imported/used by other modules or external triggers.

### **Infrastructure & DevOps**
- **Hosting:** Vercel (Frontend)
- **Database:** Supabase Cloud
- **Testing:** Vitest (Unit/Integration) configured in `package.json`.
- **CI/CD:** Manual triggers currently.

## 3. Project Structure

### **Root Directory (Unconventional)**
The frontend source code is currently located in the **ROOT** directory, not inside `src`.
- `/components`: UI Components (26+ files).
- `/pages`: Route components (`Dashboard.tsx`, `Voters.tsx`, etc.).
- `/hooks`: Custom hooks (`useVoters`, `useAuth`).
- `/contexts`: Global state providers.
- `/lib`: Utils (supabase client).
- `App.tsx` / `index.tsx`: Entry points.

### **`src/` Directory**
Contains secondary/utility structures:
- `/src/components`, `/src/contexts`, `/src/utils`.
- **Note:** Likely a remnant or a target for refactoring. Main active code is in Root.

### **`app/` Directory (Python Backend)**
- `/app/models`: SQLAlchemy definitions (`cabinet.py`, `demand.py`).
- `/app/schemas`: Pydantic schemas.

### **`supabase/` Directory**
- `/migrations`: SQL files for DB schema. **Source of Truth for DB**.
- `/functions`: Deno Edge Functions source code.

## 4. Key Configuration Files
- `ARCHITECTURE.md`: Detailed "Bible" of the project. **Read this for deep domain logic.**
- `package.json`: Frontend dependencies & scripts.
- `tsconfig.json`: TypeScript config.
- `vite.config.ts`: Build config.

## 5. Development Workflow

### **Running Local Dev**
```bash
npm run dev      # Starts Vite server (Client)
npm run test     # Run Vitest tests
```

### **Database Migration**
Migrations are managed via Supabase CLI in `supabase/migrations`.
- **Always** ensure RLS policies are applied to new tables.

## 6. Current Challenges & Recommended Actions
1.  **Project Organization:** Frontend files in root vs `src` is confusing.
    - *Action:* Recommend moving root components to `src/` to clean up the workspace.
2.  **Python Integration:** Clarify how `app/` is executed.
    - *Action:* Add `main.py` or Dockerfile to `app/` if it's a standalone service.
3.  **Testing:** Vitest is set up but coverage is unclear.
    - *Action:* Run `npm run test` to gauge health.

> **Note to Agents:**
> - **Context First:** Always check `ARCHITECTURE.md`.
> - **Language:** UI/Feedback in PT-BR. Code/Logs in English.
> - **Structure:** Respect the existing root-level React structure until a refactor task is explicitly approved.
